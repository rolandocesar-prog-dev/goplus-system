<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.goplus.repartidor.goplus_repartidor">

    <!-- ========================================= -->
    <!-- PERMISOS PARA REPARTIDOR -->
    <!-- ========================================= -->
    
    <!-- Ubicación (fundamental para tracking) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    
    <!-- Internet y conectividad -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    
    <!-- Cámara para QR/códigos de barra -->
    <uses-permission android:name="android.permission.CAMERA" />
    
    <!-- Notificaciones Firebase -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- ✅ WAKE_LOCK para mantener ubicación activa (sin WorkManager) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- ❌ REMOVIDO: Permisos de WorkManager -->
    <!-- <uses-permission android:name="android.permission.FOREGROUND_SERVICE" /> -->
    
    <!-- Features requeridas -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="true" />

    <application
        android:label="GoPlus Repartidor"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:usesCleartextTraffic="true"
        tools:targetApi="28">
        
        <!-- ========================================= -->
        <!-- ACTIVITY PRINCIPAL -->
        <!-- ========================================= -->
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            
            <!-- ✅ DEEP LINKS para navegación -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                      android:host="repartidor.goplus.app" />
            </intent-filter>
        </activity>

        <!-- ========================================= -->
        <!-- FIREBASE MESSAGING -->
        <!-- ========================================= -->
        <service
            android:name="com.google.firebase.messaging.FirebaseMessagingService"
            android:enabled="true"
            android:exported="false"
            android:directBootAware="true">
            <intent-filter android:priority="-500">
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        
        <!-- ❌ REMOVIDO: Receivers y Services de WorkManager -->
        <!-- Eliminamos las configuraciones de WorkManager que causaban problemas -->
        
        <!-- ========================================= -->
        <!-- FLUTTER & GOOGLE MAPS -->
        <!-- ========================================= -->
        
        <!-- API Key para Google Maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${GOOGLE_MAPS_API_KEY}" />
        
        <!-- Flutter embedding versión 2 -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
            
        <!-- ========================================= -->
        <!-- PROVIDER PARA GEOLOCATOR -->
        <!-- ========================================= -->
        <provider
            android:name="com.baseflow.geolocator.GeolocatorLocationProvider"
            android:authorities="${applicationId}.flutter.geolocator"
            android:exported="false" />

    </application>
</manifest>